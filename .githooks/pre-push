#!/bin/sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)
cd "$repo_root"

if ! command -v lychee >/dev/null 2>&1; then
  echo "Missing required command: lychee" >&2
  exit 1
fi

lychee .
