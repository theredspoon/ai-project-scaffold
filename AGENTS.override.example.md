# Personal Codex Overrides

Personal, machine-specific overrides for Codex in this repository.

Copy this file to `AGENTS.override.md` and customize it locally. Keep `AGENTS.override.md` out of version control.

Use this file only for personal preferences or local workflow adjustments that should not change the shared project rules in `AGENTS.md`.
